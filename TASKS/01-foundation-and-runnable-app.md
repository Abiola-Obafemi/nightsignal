Foundation and runnable app
Create a production-quality Vite + React + TypeScript application named NightSignal. Establish strict TypeScript, linting, a clean app structure, reusable components, and reliable dev/build/test commands. The app must run locally and build successfully. Keep the UI dark from the first render. Add a basic test setup and document commands in README.
