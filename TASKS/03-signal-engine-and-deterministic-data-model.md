Signal engine and deterministic data model
Design and implement a typed signal/event model with deterministic demo data generation. Include timestamp, source, strength, category, status, and metadata. Add selectors/derived summaries and tests. Keep the model independent from presentation.
